export type User = {
  id: number;
  username: string;
  age: number | null;
  role: string;
};

export type JsonPlaceholderUser = {
  id: number;
  name: string;
  username: string;
  email: string;
};
